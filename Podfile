# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Job Hub' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Job Hub

  target 'Job HubTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Job HubUITests' do
    # Pods for testing
  end

end
